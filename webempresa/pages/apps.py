from django.apps import AppConfig


class PagesConfig(AppConfig):
    #default_auto_field = 'django.db.models.BigAutoField'
    name = 'pages'
    #nombre que se mostrara en el admin
    verbose_name = 'Gestor de páginas'
